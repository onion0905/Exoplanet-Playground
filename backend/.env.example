# Environment variables for development
SECRET_KEY=exoplanet-ml-secret-key-dev-change-this-in-production
FLASK_ENV=development
FLASK_DEBUG=True

# Database settings (if needed in future)
# DATABASE_URL=sqlite:///exoplanet.db

# ML Configuration
MAX_UPLOAD_SIZE=16777216  # 16MB in bytes
SESSION_TIMEOUT=3600      # 1 hour in seconds
MAX_CONCURRENT_SESSIONS=10

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# WINDOWS USERS:
# For PowerShell: Copy env.ps1 to env-local.ps1, edit values, then run: . .\env-local.ps1
# For Command Prompt: Copy env.bat to env-local.bat, edit values, then run: call env-local.bat
# For Unix-like (Git Bash/WSL): Copy this file to .env and use with python-dotenv